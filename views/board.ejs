<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="refresh" content="2">
        <meta charset="utf-8"/>
        <meta name="description" content="Gra w kółko i krzyżyk"/>
        <meta name="keywords" content="gra, kółko, krzyżyk"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Kółko i krzyżyk</title>
        <link rel="stylesheet" href="css/style.css"/>
    </head>
    <body>
        <% 
            let messages = ["Teraz krzyżyk", "Teraz kółko", "Oczekiwanie na gracza...", ];
        %>
        <h2 id="message">
            <% if (board.draw == true) { %>
                <span class="active"><%= "Remis!" %></span>    
            <% } else if (board.winner != null) { %>
                <span <%= playerNumber == board.winner ? "class=active" : "" %>><%= "Wygrywa "+board.nicks[board.winner]; %></span>
            <% } else { %>
                <%= messages[board.turn]; %> 
            <% if (board.turn == 0 || board.turn == 1) { %>
                <span <%= playerNumber == board.turn ? "class=active" : "" %>><%= "("+board.nicks[board.turn]+")"; %></span> 
            <% }} %>
        </h2>
        <form method="post">
            <div id="board">
                <button name="b" value="0"><%= board.b[0]; %></button><button name="b" value="1"><%= board.b[1]; %></button><button name="b" value="2"><%= board.b[2]; %></button>
                <button name="b" value="3"><%= board.b[3]; %></button><button name="b" value="4"><%= board.b[4]; %></button><button name="b" value="5"><%= board.b[5]; %></button>
                <button name="b" value="6"><%= board.b[6]; %></button><button name="b" value="7"><%= board.b[7]; %></button><button name="b" value="8"><%= board.b[8]; %></button>
            </div>
        </form>
    </body>
</html>